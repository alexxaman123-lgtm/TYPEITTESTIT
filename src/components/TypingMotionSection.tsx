interface TypingMotionSectionProps {
  locale?: string;
}

/**
 * The former floating keyboard-tile showcase was intentionally removed.
 * GOATTYPE now uses its wordmark and existing goat symbol as the single
 * recognizable brand detail, keeping the product experience calm and focused.
 */
export default function TypingMotionSection(_props: TypingMotionSectionProps) {
  return null;
}
